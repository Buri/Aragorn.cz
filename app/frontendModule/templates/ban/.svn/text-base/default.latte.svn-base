{$title='Ban'}
{block content}
<h2>BAN</h2>
<div class="pre">Byl vám udělen ban (zákaz vstupu na server):
    <strong>Důvod:</strong> <em>{$data["reason"]}</em>
    <strong>Uživatel:</strong> {$data["username"]} ({$data["ip"]})
    <strong>Udělil:</strong> {$data["author"]} ({$data["time"]})
    <strong>Vyprší:</strong> {$data["expires"]}
Po serveru se můžete až do vypršení banu pohybovat jako nepřihlášený uživatel.
</div>