{extends '../../templates/@layout.latte'}
{$title='Registrace'}
{block content}
    <h1>Registrace</h1>
{if NEnvironment::getVariable('registrationEnabled')}
{include #innercontent}
{else}
<div>Registrace je uzavřena.</div>
<div>{$closeReason = NEnvironment::getVariable('registrationCloseReason')}</div>
{/if}